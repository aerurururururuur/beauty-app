# modules/shared/application —— 应用层

本层放「跨模块共享的应用服务/用例」。

- **现状**：空。shared 目前是纯类型/常量/错误地基，无独立应用逻辑；唯一的运行态装配发生在 `compose.ts`（占位，返回 `ready: true`）。
- **将来放什么**：若出现需要跨模块共享的应用服务（日志器、时钟、统一 ID 生成等），由本层暴露、`compose.ts` 里返回实例。
- **别放什么**：业务规则——那是各业务模块自己的事，别把共享模块变成垃圾桶。
