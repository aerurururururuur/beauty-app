/**
 * application/system-prompt.ts —— 系统提示的组装(**纯函数**)。
 *
 * §7.3 第 7 条:上下文管理上,最该留住的是 **`LookSpec` 的当前值和用户偏好,
 * 不是全部聊天原文**。所以这里每轮把这两样**重新拼**进系统提示,
 * 而不是指望模型从几十轮历史里自己翻出来。
 *
 * ★ **它不进 `messages[]`**(见 `entities/session.ts` 文件头):
 * 派生出来的东西不存第二遍,否则历史里那份会和当前状态自相矛盾。
 *
 * ★ **提示里必须诚实说出图那件事的规矩:调用不等于出图。**
 * 出图能力已经接上了(§14 阶段 3),但它是**人在回路**的:模型调 `render_look` 只是
 * 把请求交给用户确认,用户点了才会真的花钱生成。不写这一句,模型多半会走两个极端——
 * 要么顺着用户说「好的这就给你出图」然后就当出好了(用户等一个永远不来的图),
 * 要么反过来还说自己不能出图(能力明明有了,却被它自己藏起来)。
 * 所以这一节钉的是:**它必须停下等人,且不许替人拍板。**
 *
 * ⚠️ **但"钉住一句话"本身翻过车,教训留在 `v5` 的沿革里**:光把句式写给模型、
 *   不把**前提**和**状态**一起给它,它就会把那句话当成一个可以随时使用的说法。
 *   所以现在规则 6 是**有前提的**,而且当前状态里有一行专讲出图走到哪了
 *   (见 `render-state-description.ts`)。**句式、前提、状态,三样缺一样就会被绕过去。**
 */
import type { Session } from '../domain/entities/session.js';
import { describeBrief } from './brief-description.js';
import { describeLookState } from './look-state-description.js';
import { describeRenderState } from './render-state-description.js';

/**
 * 提示版本号。★ 随每次 LLM 调用记进夹具(同 §5.2 对 `prompt-builder` 模板版本号的要求):
 * 「没有它,过两天没人说得清这次回复是哪版提示词出来的。」
 *
 * ⚠️ **改措辞就必须动它**,否则同一个版本号会对应两段不同的文字,那正是这个常量要防的事。
 *
 * 版本沿革(每一版只记"改了什么、为什么",不重述正文):
 * - `v1` —— 出图接线之前。那一版第 6 条写的是"还不能出图"。(**没有录过夹具**,
 *   LLM 的 record/replay 本身还没做。)
 * - `v2` —— 出图接线,第 6 条改口径。留这个界碑的意思是:从这一版起按"引擎已接上"说话。
 * - `v3` ——(2026-09-16)接入产品库:加第 7 条(推荐规则 + 天气缺失的退路)。
 *   之所以**必须**动版本号而不是只加几行:第 7 条改变了模型的**行为面**
 *   (它现在会去调 `list_products`,并在正文里出现品牌产品名),同一个 `v2` 对应
 *   两种行为会让"这轮回复是哪版提示词出来的"这个记录失去意义。
 * - `v4` ——(2026-09-16,同一天)**收窄第 7 条的触发条件**:从"用户想知道就推"
 *   改成"**读了才算数的那一步必须由用户开口**"。人拍板的原话是「这个推荐产品不是
 *   agent 自动去读的……如果用户还需要,那么 agent 才会去读」。
 *   **改动理由见 `LIST_PRODUCTS` 的文件头**(那里解释了 `v3` 那种写法为什么看起来
 *   更聪明、实际却变成主动推销)。
 *
 *   ⚠️ **本条公布后又被改了一次措辞,故意并进 `v4` 而没有另起 `v5`。**
 *   第一版写成"连问都不许问",人随即纠正「可以主动挑话头」,于是改成
 *   **"问随时可以,读要等用户开口"**。并进来的理由:那一版**没有经过任何一次真实调用**
 *   (当天没有用 `v4` 跑过一轮,也就没有哪份夹具对应它),另起 `v5` 反而会在版本沿革里
 *   留一个"存在过但谁也不认识的版本"。**如果实际上拿 `v4` 的初版跑过,这条就该拆成 `v5`。**
 *   ★ 注意 `v3 → v4` 才是真正的行为分界线(v3 会**读完再推**,v4 只在用户开口后读);
 *   初版与新版的差别只是"允许先问一句"。
 * - `v5` ——(2026-09-16,同一天)**把出图的"状态"交给模型,并给规则 6 那句示范话
 *   加上前提**。起因是当天用真实模型跑端到端时的一次翻车:妆面聊定、用户还没说要出图
 *   的那一轮,模型回了一句「确认之后我就开始出图」,而它**没调 `render_look`**、
 *   会话里根本没有确认框——用户去找一张不存在的卡片。
 *
 *   根因不是模型不听话,是**规则 6 只给了句式、没给前提**(那句示范话还被原样印在
 *   `RENDER_LOOK` 的描述里,模型每轮都读得到),而 `## 当前状态` 里**一个字都没提**
 *   出图走到哪一步。所以这一版加了两样:规则 6 把前提说死,当前状态里加一行
 *   `describeRenderState()`(照片 / 已出图数 / **此刻有没有确认框**)。
 *   ⚠️ 前提**不是**"等工具返回了那句结果再说"——有 `pending` 时结果被丢弃
 *   (见 `agent-loop.ts` ⑦),模型根本看不到,那样写就是一句做不到的指令。
 *
 *   ★★ **加完之后又跑了一轮真实端到端,翻车换了个形状又出现了一次,于是前提补了半句。**
 *   那一轮模型**确实调了** `render_look`,只是工具因为**还没收到照片**而报错——
 *   它照样说了「确认之后我就开始出图」。也就是说"和调用绑在同一条回复里"这个前提
 *   **在字面上被满足了**(报错那一轮它是在下一轮才说话的,严格说不满足,
 *   但它没有按字面去推)⇒ 前提必须**再补一条它一眼就能核对的**:
 *   **这次调用没有报错**。落地方式不是再写一句规则,而是**在那句报错里直接告诉它**
 *   (见 `observations.ts` 的 `NO_CONFIRMATION_NOTICE`)——报错那一刻它手里正拿着
 *   那句 `tool_result`,离得最近的话最管用。
 *
 *   ⚠️ **这一版一次真实调用之后又改过,仍然并进 `v5`,没有另起 `v6`。**
 *   判据照旧是"有没有被真实调用过":改动前的 `v5` 措辞确实跑过一轮端到端,
 *   但**同一轮里就发现它不够**,还没有任何一份"属于那一版的"可复用夹具,
 *   而且这一版**从没交付出去过**(改完才发现,演示与前端都还没用它)。
 *   如果 `v5` 曾经上线过,这里就该拆成 `v6`。
 *
 *   ⚠️ **这一版没有并进 `v4`,与上一版的处理正好相反——区别在于"有没有真的跑过"。**
 *   `v4` 那次改措辞时初版一次真实调用都没跑过,所以并进去不留"谁也不认识的版本";
 *   而 `v4` 这次是**拿真实模型跑过端到端**的(当天 4 轮 + 3 轮两轮,翻车就是它跑出来的),
 *   它有夹具、有实证,改动也改变了模型的**行为面**。改它必须另起版本号。
 *   ⇒ **判据是"这一版有没有被真实调用过",不是"改得大不大"。**
 * - `v6` ——(2026-09-16,同一天)**撤掉那句示范话,改成"讲事实,不给句式"。**
 *   补完第二条前提之后又跑了一轮真实端到端,翻车**第三次**出现,而且换了个形状:
 *   那一轮模型确实调了 `render_look`,工具因**还没收到照片**而报错,它**照样**说了
 *   「确认之后我就开始出图」——尽管那句 `tool_result` 里就钉着 `NO_CONFIRMATION_NOTICE`。
 *
 *   ★ 三次栽在同一件事上,才看清根因不在"前提写得够不够细",而在
 *   **我们递给模型的是"一句话",不是"一件事"**:那句话被原样印在 `RENDER_LOOK` 的描述里,
 *   规则 6 又引了一遍,于是它手里有了一个**随时可套用的句式**,而"什么时候不该说"
 *   永远是更弱的一条。**给它句式,它就会用句式。**
 *   所以这一版**把句式删掉**,只讲清要对用户说的事是什么(请求已交给他确认、要他点一下),
 *   措辞交给它自己组织——它就没有可照抄的东西了。
 *
 *   ★ 同日另修 `propose_look` 的失败文案(见 `propose-look.ts`)。实测里**它报错后模型
 *   没有重试,而是在正文里把一套妆面讲成已定**,`lookSpec` 一直空着,
 *   直到"出图"那一轮才在 `render_look` 那里塌下来。
 *   **两个 bug 是同一条链**:一次"什么都没记下却没说清"的失败,会把后面的状态一路带歪。
 *
 *   ⚠️ **这一版是"改措辞就动版本号"的正例**:`v5` 已经被真实调用跑过(翻车就是它跑出来的),
 *   按本文件立的判据它就不能再改,只能另起一版。`v5` 内部并的那次是"没跑过",
 *   这一次分开是"跑过了"——**同一条判据的两个方向**。
 * - `v7` ——(2026-09-16,同一天)**不再要求模型转述那个确认框**。`v6` 之后的第三轮实测里,
 *   "调了却报错、却仍然对用户提确认框"那一支**仍然会出现**(报错里明明钉着
 *   `NO_CONFIRMATION_NOTICE`,它还是提了)。同时观察到另一件事:用户说「好,就这样吧」时,
 *   模型**很爱调 `render_look`**——而那一轮往往还没有照片,于是必然报错,正好落进上面那一支。
 *
 *   ★ 走到这里才看清:前四版都在**加强"什么时候可以说"**,却一直没问
 *   **这件事到底为什么要模型来说**——那个确认框是**界面自己弹的**,话也是界面自己写的
 *   (`renderConfirmationSummary`)。模型被要求转述一个**它看不到也管不着**的 UI 状态,
 *   于是每轮都在拿上下文里那点线索猜,猜错就把用户指向一张不存在的卡片。
 *   ⇒ 这一版**把这项义务整个取消**:规则 6 与 `RENDER_LOOK` 都不再要求它交代确认框,
 *   只留一条禁令(别承诺出图、别催用户点确认、别交代框的状态)。
 *   **没有义务,就没有"说错时机"这回事。**
 *
 *   ⚠️⚠️ **如果这一支还会复现,下一步不该是第五次改措辞,而是改代码。**
 *   已经用掉的手段:给状态(`describeRenderState`)、给条件(规则 6 ①②)、
 *   在失败里当场提醒(`NO_CONFIRMATION_NOTICE`)、撤掉句式(`v6`)、撤掉义务(`v7`)。
 *   这些都还是"请求模型别说";它不听时唯一可靠的办法是**结构上让它说不了**——
 *   要么在循环里拦截这句(动模型输出,风险与代价都不小,须先想清楚),
 *   要么继续把确认框变成**纯界面的事**(它本来就该是)。
 *   ★ **判据是"这一支还会不会复现",不是"措辞还能不能再改细"。**
 *
 *   ✏️ **2026-09-16 晚:上面那句预言的下半截发生了 —— 这次就是"改代码"。**
 *   起因是另一次真实模型实测(用户两轮明确要图:「生成图片我看看」「出图」),
 *   模型**一次都没调 `render_look`**:它把妆面又讲了一遍,问「这套妆面你感觉怎么样?」。
 *   也就是说 v5–v7 治的"它抢着替界面交代那个框"**换了个地方发作**——
 *   禁令只关掉了"提确认框"这个出口,它征求同意的冲动落到了**妆面**上,而用户手上
 *   既没有图、也没有任何别的出入口。
 *   ★ 所以这一次**一个字的提示词都没改**(规则 6 与 `SYSTEM_PROMPT_VERSION` 都是原样),
 *   动的是结构:出图多了一条**由界面按状态自己摆**的入口(`ConfirmRender` 的入口 B,
 *   见那个文件头与 `turn-view.mapper.ts` 的 `renderOffer`)。
 *   **"用户要图"这件事不必再由模型转达,它自然就没法漏掉。**
 *   ⇒ 这条沿革到此为止:第 6 条再出问题,该看的是那条链路,不是这段话。
 * - `v8` ——(2026-09-16,同一天)**产品从"可以问一句"改成"主动挑话头",并加上由头触发。**
 *   起因:真实模型四轮实测里,`v4` 立的那句「妆面定下来之后,你可以问一句要不要顺便挑几支产品」
 *   **一次都没触发过**。那句话本身没写错,错的是它**是个许可**,而同一轮里还压着
 *   "要不要出图""先给我一张照片"这些更硬的活——**许可输给义务**。人拍板:可以主动一点。
 *
 *   所以这一版把"问"从一个许可改成**两种触发**:
 *   ① **锚点**——提出妆面那一条回复里必须撂一句产品话头。★ 时机是确定的(它刚调完
 *      `propose_look`),所以**不需要新增任何会话状态**,也就不会重演 `v5` 那种
 *      "给了义务、没给前提"的形状;
 *   ② **由头**——用户**这一轮自己**带来了对得上的由头(天气变了、抱怨脱妆卡粉、说预算、
 *      提过敏或肤质、问价钱),且库里**确实有对得上的品类**时,可以就地再提一句。
 *
 *   ⚠️ **"由头"是这一版最大的风险面,也正是它和推销之间的那条线**:由头是模型自己判的,
 *   而它想推的时候总能找到由头。所以配套钉了三条**逐条可查、不靠计数**的约束:
 *   **不许自己制造由头**(她没提天气,就不许拿天气说事)、**没对上就不提**、
 *   **同一个由头只说一次、她没接就不再提**。
 *   ★ **刻意没有写"整场最多 N 次"**:那要求模型数得清自己说过几次,而我们没有这个状态
 *   ——**给义务不给状态**正是 `v5` 翻车的形状。这里用**由头**当闸门,不用计数当闸门。
 *
 *   ★★ **"读库"那扇门一个字没动**:由头是"提一句"的理由,**不是"读"的理由**。
 *   这与 §13-6 直接相关——推荐一旦变成"妆面做完自动读库、再顺口推两支",就是主动推销。
 *   ⚠️ **如果实测里它变成每轮都提,下一步不是再补一句"别太频繁",而是把"由头"
 *   收窄成一张白名单**(天气/脱妆/预算/过敏/问价),或者把这道理序挪进代码。
 *   ★ **判据是「它会不会在没有由头的轮次也提」,不是"语气还能不能再收一收"。**
 *
 *   ⚠️ **这一版没有并进 `v7`**,尽管是同一天改的:`v7` 被真实调用跑过(三轮端到端就是它
 *   跑出来的),按本文件立的判据它就不能再改。**判据是"有没有被真实调用过",
 *   不是"改得大不大"——`v4` 那次是同一条判据的另一个方向。**
 * - `v9` ——(2026-09-16,同一天)**内部标识符不许进给用户看的正文。**
 *   起因是 `v8` 的两轮实测各抓到一个:**第一轮**模型把产品 id 缀在产品名后面当注释
 *   (「小金条口红（`42-rouge-pur-couture-the-slim-matte`）」),**第二轮**它把工具名
 *   说给了用户(「我就可以调用 `render_look` 生成成片图了」)。
 *   同一个毛病的两种长相:**它把自己读到的标识符原样抄进了人话**。
 *
 *   ★ 这条**不是模型乱来**:提示词与工具描述通篇都在用 `` `render_look` ``、`` `list_products` ``
 *   称呼这些工具,`read_product` 返回的资料里也带着 id——**我们就是这么跟它说话的**,
 *   它照着学了一遍。所以措辞上要把这件事说清楚:**那些名字是给它调用用的,不是给用户看的**。
 *
 *   ⚠️ 放进了「说话方式」而不是硬规则那条编号列表:它是**措辞口径**,不是一条会改变
 *   行为面的规矩,而 `1`–`7` 的编号被 README 引作「七条硬规则」,不动它更省事也更准。
 * - `v10` ——(2026-09-16,同一天)**删掉 `v8` 给产品话头写的那句示范话。**
 *   `v8` 原文里有一句"像「这套妆要用到的几支,回头我可以按你那边天气和肤质给你挑出来,
 *   想看了说一声」"。实测(第二、三轮)抓到了它最典型的坏处:**模型把这句话当成一个
 *   可以随时套用的句子**,于是——
 *   ① 它**没有**出现在该出现的那一轮(提出妆面那一条回复里没提产品);
 *   ② 它**出现**在了最不该出现的那一轮:**用户已经开口要产品了**(那一轮模型把
 *      `list_products` + 4×`read_product` 都调完了),正文却是这句现成话,
 *      ——**产品一条没给**,用户问了等于没问。读了五条资料,回了一句"我可以替你挑"。
 *
 *   ★★ **这是 `v6` 那条教训的原地重演,只不过换了个位置**:`v6` 是从**规则 6**里
 *   删掉「确认之后我就开始出图」,`v10` 是从**规则 7**里删掉这句产品话头。
 *   两次的病一样——**给模型一个句式,它就会把句式用在不该用的地方**,
 *   因为"什么时候不该说"永远是更弱的一条。**要钉的是"这件事办没办",不是"这句话说没说"。**
 *   ⚠️ 中文提示词里写"像……"就等于写了一句可抄的范例:**想给模型举例子,是最自然的冲动,
 *   而它两次都是 bug 的成因。**
 *
 *   ⚠️ **这一版仍然只改措辞,而且这是第三次**。同时记下同一轮实测里另外两处**没有**被
 *   禁令拦住的东西(都不是本版能修的,留给"要不要动代码"那个决定):
 *   · 规则 6 **已经逐字禁过**的「我这就给你出图」还是原样出现了(第三轮);
 *   · `v9` 刚立的"不许复述工具名"当场被破(同一轮:「等你点头再调 `render_look` 哦」)。
 *   ⇒ 这两条都属于**"请求模型别说"已经用到头**的形状。**再加第五、第六句禁令,
 *   预期收益接近于零**;真要拦,只有动输出或让那件事不再需要它说。
 * - `v11` ——(2026-09-16,同一天)**妆面的失败也要跨轮活下来。**
 *   ⚠️ **这一版不是措辞改动,是补状态**——加了一个 `describeLookState()`
 *   (文件在 `look-state-description.ts`),`## 当前状态` 里那一行"妆面"从此由它出。
 *
 *   起因是 `v5` 那天记下的那个 bug**又露面了**:`propose_look` 被拒之后模型没重试,
 *   在正文里把一套妆面讲成已定,`lookSpec` 空着。`v5` 当时修的是**工具自己那句失败文案**
 *   (「★ 这次**没有记下任何妆面** …请修正后**再调用一次**」),事后诊断证明**它在当轮有效**
 *   ——有一次实测里模型确实在**同一个回合内**重试并成功了。
 *
 *   ★★ **它漏掉的是"这一轮之后"**:那句提醒只活在当轮的工具结果里。
 *   模型若照旧用正文讲完就收尾,**下一轮的系统提示只有一句「当前还没有提出过妆面」**
 *   ——准确,但读不出"你上轮被拒了"和"**正文里写了不算**"这两件事。
 *   于是它以为自己提过了,再也不会去调那个工具,而用户手上什么都没有。
 *   ⇒ 与出图那边(`describeRenderState` / `NO_CONFIRMATION_NOTICE`)同一个道理:
 *     **关于状态的事实,要放在每一轮都读得到的地方,不能只放在当场那句回执里。**
 *
 *   ⚠️ 这一行**在 `lookSpec` 有值时也会报失败**(形状:定稿成功 → 用户要改 → 改的那次被拒,
 *   会话里留着的还是**老的那套**)。只按 `lookSpec` 有没有来分支的写法会给模型显示
 *   "一切正常",而它上一条工具结果说的是"这次没记下"——**两个说法对不上,它就可能跟用户说
 *   "改好了",而渲染用的仍是老妆面**。所以那一支要明说"老的还留着"。
 *
 *   ⚠️ **写完这一版后立刻跑了一轮真实模型,当场改了两处,记在这里**(判据"跑过就得另起一版"
 *   对的是**已定稿并发出去**的版本;这一版还没合入,所以是**就地补**):
 *   · 实测首轮出现的是**另一种形状**:模型**一次 `propose_look` 都没调**,
 *     正文里却把一整套妆面讲完了(`lookSpec` 空)。那一支**没有失败的结果块**,
 *     所以"上一次被拒了"那半句**够不着它**——于是空格那一支也补了
 *     「★ 这一格只认 `propose_look` 成功——**正文里描述过妆面不算数**」。
 *     ★ 两次实测里它**都只调了 `patch_brief`**:需求那一步进得了状态,妆面那一步进不了。
 *   · 同轮第二轮:模型 `propose_look` **报错后重试成功**(与更早那次诊断一致),
 *     说明**当轮那条失败文案确实在起作用**——v11 补的只是它够不到的那一段。
 *
 *   ★ **判据仍是"这一支还会不会复现"**:若模型还是在正文里把妆面讲成已定,
 *   下一步是**在循环里拦**(一圈终了时 `lookSpec` 仍空、而正文描述了妆面 → 不让收尾),
 *   而不是第四次改措辞。这条留给"要不要动代码"那个决定。
 *
 * ── ✏️✏️ 2026-09-16 晚:**同一支第三次露面,但形状比上面两次更靠前** ────────────────
 *
 * ★★ **这一次"一个工具都没调",连 `patch_brief` 都没有**(`brief` / `lookSpec` 都是空的)。
 *   上面 `v11` 那段记的是"只调了 `patch_brief`、妆面那一步进不了状态";
 *   这次是**整层工具都没进过**。两件事的修法不一样,别混着读。
 *
 * ⚠️ **根因不是措辞,是规则 3 的后半句**「肤色不许猜。**不知道就用一句话问清楚。**」
 *   模型把用户说的「偏深」判成"不知道是哪一档",于是**把这一轮整个用来反问**
 *   (实测原话:「你说"偏深",是指接近「深色」(deep) 这一档,还是「中等偏深」(tan)？」)
 *   —— 反问就收尾了,`patch_brief` / `propose_look` 一次都没轮到。
 *   ★ 规则 3 的**前半句(不许猜)是要保的**(挨着红线 §13-3);坏的是"否则就不动"这个形状。
 *
 *   **实测对照**(`qwen-plus`,同一句用户原话「我下周有个面试，肤色偏深、混油皮…」,
 *   只改 system 提示词;`buildSystemPrompt` 可离线重建,完整版 2324 字与真机发出去的那份逐字相同):
 *   | system 提示词 | 长度 | 结果 |
 *   | --- | --- | --- |
 *   | 完整 | 2324 | 不调工具,反问肤色 |
 *   | 角色 + 你的工作 | 119 | ✅ `propose_look` |
 *   | 角色 + 你的工作 + 规则 1–5 | 499 | 不调工具 |
 *   | 规则 1,2,4,5(**只少规则 3**) | 460 | ✅ `propose_look` |
 *   | 只留规则 5 | 238 | ✅ `propose_look` |
 *   第三、四行**只差规则 3**,一去掉工具调用就回来。
 *   ⚠️ 每个变体只跑了 **1 次**,采样不确定;要钉死得重复几遍。旁证是完整版那句反问
 *   正好是规则 3 的字面执行。控制组另证了两件事:**去掉 system 提示词** → `finish_reason: tool_calls`;
 *   **`tool_choice` 指名 `propose_look`** → 它能填出合法入参 ⇒ 端点、模型、6 个工具的 schema 全是好的。
 *
 *   ⚠️⚠️ **本次一个字都没改,是刻意的**(用户 2026-09-16 拍板"先不改,单独排期"),理由有两条:
 *   ① 改它要动**已合入、已被真实模型跑过**的 `v11` 正文 ⇒ 按本仓判据**得另起一版 `v12`**,
 *      不能就地补(那条例外的适用面是"这一版还没合入");
 *   ② 提示词改动该配**一轮完整的真机验证**,不夹在出图那次改动里做。
 *
 *   ★ **留给下一次的起点**:最小改法是**只改规则 3 的后半句**——「不许猜」留着,
 *   把它从"否则就不动"改成"**边问边记**"(先把已知的用 `patch_brief` / `propose_look` 记下来再问),
 *   这样这一轮不再空转。另一种是"允许缺省"(肤色未知时按 `medium` 档先出妆面,
 *   `brief.ts` 的缺省本来就是 medium),代价是可能白出一版。
 *   ⚠️ 顺带一个**下游后果**,别只盯着妆面:`lookSpec` 空 ⇒ `renderReadiness !== 'ready'` ⇒
 *   界面那条 `renderOffer` 摆不出来 ⇒ **真机上用户拿不到图**。出图那条链本身是对的
 *   (入口 A/B 都用真实 1.1 MB 照片跑通过),挡住它的是**这一步**。
 *
 *   ★ **零成本复现手法(下次别再花调用去猜)**:`AGENT_BASE_URL` 可以覆盖端点,
 *   指向本地一个只落盘、只回假 completion 的 echo 服务,就能拿到**服务端实际发出去的请求体全文**
 *   (`model` / `tools` / `tool_choice` / system 长度),**0 次 LLM 调用**;
 *   确认"发出去的是对的"之后,再把那份请求体**原样重放并逐个改动**,每次 1 次调用、单变量。
 * - `v12` ——(2026-09-17)**只改规则 3 的后半句:把"否则就不动"改成"问的同时把这一轮该做的做完"。**
 *   这是上面 `v11` 那段留的"下一次"——它当时定的是**最小改法**,本次照做,别的一字未动。
 *
 *   ★★ **先说结论:上一版那个"一个工具都不调"确实复现了,而且这次是 3/3 复现。**
 *   `scripts/probe-agent-prompt.ts`(`npm run probe:agent-prompt`)——
 *   同一句用户原话、同一批工具、**只替换规则 3 那一条**,每个变体跑 3 次:
 *
 *   | 变体 | 规则 3 的改法 | 落下 `lookSpec` | 记进 `brief` 的肤色 |
 *   | --- | --- | --- | --- |
 *   | `v11`(现状) | 不知道就用一句话问清楚 | **0/3** | — |
 *   | `v12a` | +"问的同时把该做的做完" | 3/3 | ⚠️ **deep(猜的)** |
 *   | `v12b` | `v12a` +「偏深」不算说清楚 | 2/3 | ⚠️ tan(猜的) |
 *   | `v12c` | 后半句改成"照常往下走" | 3/3 | ⚠️ **deep(猜的)** |
 *   | ★ `v12d`(采用) | `v12a` + **"妆面不需要肤色"** + 肤色显式留空 | **2/3** | ✅ **空(没猜)** |
 *
 *   ★ **为什么采用一个命中率更低的那版——这是本版最该看懂的一处。**
 *   `v12a`/`v12c` 的 3/3 是**假的**:它们靠**替用户把肤色定了**(`patch_brief` 里写死 `deep`)
 *   换来"这一轮不空转",而那正是规则 3 前半句禁止的事、也直接违反 §6 规矩 4。
 *   代价当场就能看见——那两版的工具序列**全是** `propose_look✗ → propose_look✓`:
 *   第一版妆面按猜出来的肤色之外的颜色配,**被按肤色收窄的校验器整个打回**,
 *   靠模型自己读了报错再改一次才勉强成立。
 *   ⇒ **只数 `tool_calls` 会把这个失败读成成功**,所以探针跑的是真循环 + 真工具,
 *     量的是"会话里落没落下一份妆面"(见脚本文件头)。
 *
 *   ⚠️ **`v12b` 那条不能加,尽管它听起来最"精确"**:把「偏深」点名成"仍不知道是哪一档"
 *   之后,模型**又退回到只反问**(3 次里 2 次整轮一个工具都不调)——即 `v11` 的形状。
 *   **"告诉它这里含糊"和"让它别停下来"是两股相反的力**,加了前者的措辞会把后者顶掉。
 *
 *   ⚠️ **采用的 `v12d` 仍有 1/3 落空**,形状是**旧的另一种**(调了 `patch_brief`、
 *   正文里把妆面讲成已定、`propose_look` 一次没调)。这一支**够不着本版**:
 *   它不在"问不吃掉整轮"那条线上。**但它是可自愈的**——
 *   `describeLookState()`(`v11`)在下一轮会明说"正文里描述过妆面不算数",
 *   用户随便再回一句,妆面那一步就会补上。**没有把它当成已解决。**
 *
 *   ⚠️ **样本量如实记:每个变体 `n=3`、单模型(`qwen-plus`)、单句用户原话。**
 *   它比上一版的 `n=1` 硬,但**远不到"钉死"**——`v12a` 与 `v12d` 的差距(3/3 vs 2/3)
 *   就在采样噪声的量级上,真正把它们分开的是**"有没有猜肤色"这条不留噪声的判据**(看 `brief`)。
 *   ⇒ 下次再复现,先看 `brief.skinTone` 有没有值,再看 `lookSpec`。
 *   ★ **要重跑就重跑那个脚本,别再手工改提示词试**:它零成本复现(`--dry-run`)、
 *   单变量、且产物落盘成夹具(`out/probe-agent-prompt/<时间戳>/`)。
 * - `v13` ——(2026-09-17)**推荐产品时不许交代取数过程;`id` 的禁令收成一句不含位置信息的话。**
 *   起因是一次真实模型实测跑通之后用户提的两条,都在**给用户看的那段正文**里:
 *   ① 它写「我马上从库里给你挑几支真正适配你皮肤状态 + 面试需求的产品」——
 *      **把自己的工作流程讲给了用户**(用户原话:「不能让 ai 说明自己是去从库里找,应该单纯做推荐」);
 *   ② 它推产品时写成「`33-touche-clat-le-teint`(妍活青春/逆龄粉底液)」——
 *      **id 摆在了产品名的位置上**(用户原话:「不要产品输出英文名而不中文名」;
 *      ⚠️ 那是 id,不是英文名——库里 57 条的名称本来就是中文在前,#44 那条除外,见下)。
 *
 *   ★★ **① 的根因就在本文件里,而且是"我们自己教它的"。** 规则 7 那段话头写着
 *   「让她知道**你能按天气和肤质替她挑几支产品**、想看了跟你讲一声」——
 *   模型把那半句原样学走了,只是**给它补了一个来源**(它知道自己有个库,于是加上了「从库里」)。
 *   ⚠️ 这与 `v10` 删掉那句示范话是**同一个坑的另一半**:`v10` 治的是"句式被用在不该用的轮次",
 *   这一次是"句式里的**内容**被复述给用户"。**话头本身要说,但"我从哪儿取数"不属于要说的事。**
 *   所以本版不是再加一句禁令了事,而是**把两件事分开讲**:话头是**问她要不要**(还要说),
 *   过程交代是**该给的时候还在讲自己怎么找的**(不许说)。
 *
 *   ★★ **② 的根因不在本文件,在工具输出的版面上**——所以那半个改动在
 *   `application/tools/list-products.ts` 的 `renderEntry`(id 从行首挪到行尾)。
 *   这里只改了 `v9` 立的那句话,因为**它当初拦错了形状**:
 *   原文写的是「别把 id **缀在后面**当注释」,而实测里 id 是出现在**前面**的。
 *   ⇒ `v6` / `v10` / 这一条是同一句话教三遍的事:**给一个具体的反面例子,
 *   模型就只绕开那个形状**。所以改成不带位置信息的封闭说法(前/后/括号里,都一样不行)。
 *
 *   ⚠️ **本版尚未经过真实调用**(改动当天没有配额跑)。上面两条都只是"改完之后的预期",
 *   **不是实测结果**——按本仓口径记成 `[未验证]`。要验证,最小的一轮是:
 *   一句「我下周面试,肤色偏深、混油皮」+ 一句「那我该买什么」,看**最后那段正文里
 *   有没有 id、有没有「从库里」**。⚠️ 若复现,② 的下一手**不是再改措辞**(版面已经让过了),
 *   而是在循环里拦正文;① 的下一手则值得回头问"那个话头是不是根本不该由模型说"。
 */
export const SYSTEM_PROMPT_VERSION = 'v13';

export interface SystemPromptOptions {
  /**
   * 本轮有没有注册产品工具(`list_products` / `read_product`)。
   * 缺省 `false` = 这个部署没有产品库。
   *
   * ★ **由调用方从注册表现算,不另存一个配置布尔值**(见 `agent-loop.ts` 那行):
   *   注册表本来就是"哪些工具真的存在"的唯一出处。另存一个 `AGENT_HAS_PRODUCTS`
   *   之类的开关,就多了一处**能和现实对不上**的东西——而它对不上的后果很具体:
   *   提示词让模型去调一个不在工具名单里的工具,模型照做,拿回一句
   *   「没有名为 list_products 的工具」,然后它就自己编产品了。**那正是红线 §13-6 要防的。**
   */
  hasProducts?: boolean;
}

export function buildSystemPrompt(session: Session, options: SystemPromptOptions = {}): string {
  // ★ 妆面那一行的出处见 `look-state-description.ts` 的文件头:
  //   它多报一件事——**上一次 `propose_look` 有没有被拒**(v11)。
  const look = describeLookState(session);
  // 免费工具的名单按实际注册的写。★ 多列一个不存在的工具,比少列一个更坏(见 options 的注释)。
  const freeTools = ['`patch_brief`', '`propose_look`', '`list_cabinet`'];
  if (options.hasProducts) freeTools.push('`list_products`', '`read_product`');

  const rules: string[] = [
    '1. **你永远不写图像提示词。** 你的产出只有结构化字段,生图那一步由固定模板负责。',
    '   这不是风格选择,是这套系统能保住用户本人长相的前提。不要把描述性文字当成提示词来写。',
    '2. **颜色/质地/浓度之外的诉求,如实说做不到。**「眼睛放大一点」「脸显小」「拉长眼型」',
    '   属于形态,这套妆面表达不了——直接告诉用户做不到,不要硬塞进现有字段,也不要自己发明字段。',
    // ★ v12:后半句从"不知道就问清楚"改成"问的同时把这一轮该做的做完"。
    //   理由见文件头 v12 那段——**"问"本身没错,错的是它把整轮吃掉**。
    //   ⚠️ 别把「偏深」这类含糊说法**点名**成"仍不知道是哪一档"(试过,`v12b`):
    //   那样模型会退回只反问。要它不停下来,就不能同时告诉它"这里还缺着"。
    //   ⚠️ 这几行**行首没有缩进**,与上下几条规则不一样——**这是有意的**:
    //     探针里跑的就是这份文本(逐字),加三个前导空格就不再是"实测过的那一份"了。
    //     改这里就等于换实验条件,要重跑 `npm run probe:agent-prompt` 再改。
    '3. **肤色不许猜。** 猜错会让整套配色走偏——',
    '所以 `patch_brief` 的 `skinTone` 只有她**自己说出是哪一档**时才填;',
    '说不清是哪一档的(如「偏深」)**留空,别替她定**。缺的那一项要在回复里问一句。',
    '★ **但"问"不占用这一轮**:**妆面本身不需要肤色**——`propose_look` 里根本没有这个字段,',
    '所以这一轮该做的是:先把她说过的记下(`patch_brief`,肤色留空),',
    '再按已知信息出一版妆面(`propose_look`),最后在正文里问缺的那一项。',
    '她答了之后再记一次肤色,需要就改一版妆面。',
    '★ **不许因为缺一项就把整轮用来反问**——那样这一轮什么都没落进状态,',
    '用户手上也不会多出任何东西。',
    '4. **一次只问一个问题。** 不要一口气抛出五个问题把人问烦。',
    `5. 免费的工具(${freeTools.join(' / ')})随时可用,不需要用户确认。`,
    '6. **出图要用户点头,你替他点不了。** 妆面定下来、用户又想要成片时,调用 `render_look`。',
    '   但那一次调用**不等于出图**:它会弹一个确认框给用户,他点了才会真的开始生成(按次计费)。',
    '   ★★ **那个框由界面自己弹、自己解释,你不需要转述它,也不要向用户交代它的状态。**',
    '   调用之后(以及它报错之后)**必须停下等他**——这一轮到此为止,别的话都别说满。',
    '   ★ **别提"确认框"这件事**:不要承诺"我这就出图"、不要说"已经交给你确认了"、',
    '   也不要催他"点一下确认"。**那个框并不总是存在**(你没调、或调了但它报错时就都没有),',
    '   而用户会照着你的话去找一张永远不出现的卡片。',
    '   **不要说你已经出好了,也不要替他确认。** 用户没点,就是什么都没发生、也没花钱。',
    '   这次调用到底成了没有,看下面「当前状态」里出图那一行——**它说了算,不要靠印象猜**;',
    '   它说没有,就是没有。',
  ];

  // ★ 第 7 条只在真的有产品库时才出现(理由见 `SystemPromptOptions` 的注释)。
  if (options.hasProducts) {
    rules.push(
      '7. **推荐产品:库要等用户开口才读,但话头你可以主动挑。**',
      '   ★ **提出妆面那一条回复里,必须把话头给到**:让她知道**你能按天气和肤质替她挑几支产品**、',
      '   **想看了跟你讲一声**。这是义务,不是许可。',
      '   ★★ **别拿现成句式去说它,措辞你自己组织。**(这种话被写死过一句,结果模型',
      '   把它用在了**用户已经开口要产品**的那一轮——那一轮该做的是**把产品给她**,',
      '   而不是再说一遍"我可以替你挑"。**要钉的是这件事办没办,不是这句话说没说。**)',
      '   之后,若用户**这一轮自己**带来了由头(天气变了、抱怨脱妆卡粉、说预算、',
      '   提过敏或肤质、问价钱),而库里**确实有对得上的品类**,可以就地再提一句。',
      '   ★★ **不许自己制造由头**:她没提天气,你就别拿天气说事。',
      '   **没对上就不提**——宁可漏,不可硬凑。',
      '   ★ **她没接住就不再提;她说了不用,整场都不再提。同一个由头只说一次**,别一轮一句。',
      '   ★ **提话头时只说品类,不许报具体产品名**——名字只能来自下面那两个工具。',
      '   ★★ **由头是提话头的理由,不是读库的理由**——一句话头只买得到一句话。',
      '   用户说要、或者自己问起(如「那我该买什么」「有推荐的牌子吗」),',
      '   才调 `list_products` 拿索引和匹配速查表,再用 `read_product` 读你真正想推的那几条。',
      '   用户还没开口的那一轮,你手里**不该有库里的任何内容**——',
      '   **不要先把库读出来备着**,也不要凭你记忆里的牌子顶上。',
      '   问和读是两件事:**问,随时可以、而且该主动;读,等用户点头。**',
      '   ★ **库里的产品名只能来自这两个工具**——你本来就知道的那些牌子、那几支,**一个都不许说**,',
      '   哪怕它在现实中真的存在。库里没有合适的,就直说库里没有,宁可空着。',
      '   ★ 转述产品资料里那段「社交平台用户反馈摘要」时,**必须说明它出自品牌资料**,',
      '   不要讲成真实用户评价或中立测评。',
      '   ★ **速查表的主轴是"天气 × 肤质"**,而天气不一定知道(见下面的当前状态)。',
      '   天气缺失时**退到"肤质 + 场合"**来匹配,并**如实告诉用户精度有限**',
      '   (如「不知道你那边天气,我先按肤质和场合给你挑」),不要编一个天气出来填上。',
      // ★ v13①:实测里它写了「我马上从库里给你挑几支」——把自己的取数过程讲给了用户。
      //   ⚠️ 末句那句"不冲突"是**必须留的**:v12b 的教训是两股相反的力会互相顶掉,
      //     而这里掐的正是"话头"旁边那句话,不点破就可能把 `v8` 立的话头一并掐掉。
      '   ★ **给产品的时候直接给结果,不要交代你是从哪儿找的**:',
      '   不说「我马上从库里给你挑几支」「我去查一下资料」这类话——她要的是产品,不是你的工作流程。',
      '   (★ 这与上面那个话头**不冲突**:话头是**问她要不要**、那件事还没做;',
      '   这里禁的是**已经该给她的时候,还在讲自己是怎么找到的**。)',
    );
  }

  return [
    '你是「场合美妆」的化妆顾问,通过对话帮用户定一套适合她的妆容。',
    '',
    '## 你的工作',
    '把用户模糊的说法,收敛成一份**结构化的妆面**——只用颜色、质地、浓度这三样描述,',
    '用 `propose_look` 工具记下来,然后用文字讲给她听。',
    '',
    '## 硬规则(不是建议)',
    ...rules,
    '',
    '## 说话方式',
    '简短、具体、像柜台里那位懂行的柜姐,而不是说明书。',
    '用户说「明天面试」,别回「好的我了解了」——直接给她一套,并说清为什么这么配。',
    // ★ 存在理由见文件头 `v9`:模型会把自己读到的标识符原样抄进人话(实测两轮各一次)。
    //   ★ v13②:末句从「别把 id 缀在后面当注释」改成不带位置信息的封闭说法。
    //     ⚠️ 那句原文只堵了"后面",而下一轮实测里 id 出现在**前面**(见文件头 v13)。
    //     这一句是**兜底**;那头真正的改动在 `list-products.ts` 的版面上,别只改这里。
    '★ **我在这里跟你说的工具名和 id,是给你调用用的,不要复述给用户看**——',
    '她不知道 `render_look` 是什么,也不该看见 `31-all-hours-foundation` 这样的串。',
    '要提产品就照品牌的名字说(如「恒久粉底液」);**id 这样的串一个字都不该出现在给用户看的正文里**',
    '——放在前面当标签、缀在后面当注释、塞进括号里,都一样。',
    '',
    '## 当前状态',
    `已知需求:${describeBrief(session.brief)}`,
    look,
    // ★ 出图那一行的存在理由见文件头 v5 那段:模型过去只能靠猜,而它猜错过一次。
    describeRenderState(session),
  ].join('\n');
}
