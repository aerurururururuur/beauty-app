# 复制本文件为 .env 后使用（Vite 默认读取项目根目录的 .env）
# 演示模式：不设置或为 true 时，前端走本地 mock 数据，无需后端即可演示
#           设为 false 时，走真实后端接口
VITE_USE_MOCK=true

# 后端 API 基础路径（开发时经 Vite 代理到 http://localhost:3000）
VITE_API_BASE=/api
